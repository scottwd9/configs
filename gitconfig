[user]
    name = scottwd9
    email = scottwd9@gmail.com
[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    l = log --graph --decorate --pretty=oneline --abbrev-commit
    ll = log --pretty=format:'%h %ad | %s%d [%an]' --decorate --graph --date=short
    lla = log --pretty=format:'%h %ad | %s%d [%an]' --decorate --graph --date=short --all
    ls = ls-files
[branch]
    autosetupmerge = true
[core]
    autocrlf = input
    editor = emacs -nw
[color]
    ui = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
